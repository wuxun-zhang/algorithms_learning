#### 马拉车算法（Manacher's Algorithm）

# 查找字符串的最长回文字符串，时间复杂度为O（N）

# step1: 预处理 -> 在每个字符串的左右加上一个特殊字符（‘#’），形成新的字符串t，使得原始字符串中可能存在的回文串全部变成奇回文串，方便处理

# 添加辅助参数
p[i]：表示以t[i]字符为中心的回文子串的半径
id：最长回文子串的中心位置
mx：回文串能延伸到的最右端的位置

## 核心思路
p[i] = mx>i ? min(p[2*id-i], mx-i):1
